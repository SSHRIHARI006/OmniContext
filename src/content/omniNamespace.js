/*
 * OmniContext shared namespace.
 * Runtime source files are loaded directly by the browser in dependency order;
 * this file provides the explicit namespace used between those files.
 */
(function (root) {
  root.OmniContext = root.OmniContext || {};
})(globalThis);
